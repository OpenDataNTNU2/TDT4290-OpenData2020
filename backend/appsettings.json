{
  "_comment": [
    "WARNING - This file will be checked into source control. Do not change this file.",
    "Use this as an example file only.",
    "Use appsettings.{env.EnvironmentName}.json as your configuration file as it will not be checked into source control.",
    "{env.EnvironmentName} values : development, staging, production"
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "memory": "data-in-memory",
    "default": "data source=default.sqlite3",
    "PostgreSQL": "PostgreSQL string goes here"
  },
  "Kestrel": {
    "EndPoints": {
      "Http": {
        "Url": "http://localhost:5000"
      }
    }
  },
  "gitlabUrl": "http://gitlab.potrik.com/",
  "gitlabToken": "Token for connecting to gitlab API",
  "identifierUrl": "http://katalog.xn--sampne-kua.no:5000/api/datasets/"
}
