import React from 'react';
import { Grid } from '@material-ui/core';


const InterestCard = (props) => {

    return (  
        <b>
            {props.interestCounter}
        </b>
    )
}

export default InterestCard;